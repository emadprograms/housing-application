# Quick Task: Modern UI Layout and Settings Placement

## Description
Overhaul the dashboard user interface with a modern, cohesive, enterprise-grade layout:
1. **Modern Layout & Aesthetics**:
   - Replace outdated styles, raw emojis, and blocky elements with sleek dark/neutral palette, refined typography, and Lucide-style SVG vector icons.
   - Upgrade the sidebar with a polished brand header, segment control mode switcher (Tree, Grid, Database), and clean disclosure-chevron tree items.
   - Refine the top navigation bar with breadcrumb navigation (`Area / House`), tenure pill, search input with `⌘K` badge, and a dedicated **Settings / House Actions** button.
2. **Settings Integration**:
   - Relocate the misplaced "Manage Tenants" actions into a dedicated **⚙️ Settings** control in the house header bar.
   - Clean up the document list tabs to keep them focused on content (`Folders` and `Timeline`), keeping the settings button accessible in the house header.
3. **Modal & Component Polish**:
   - Redesign the Tenant Management dialog as the House Settings / Tenant Timeline modal with modern inputs, clean iconography, and smooth transitions.
   - Preserve all existing selectors, data attributes, and test hooks so all 50 tests continue to pass without regression.

## Tasks
1. **Frontend Architecture & Styles (`src/api/static/index.html`)**:
   - Update CSS and HTML layout structure (modern sidebar, top navbar, breadcrumbs, search input, settings trigger).
   - Replace text/emoji markers with crisp inline SVG icons (tree chevron, folders, houses, gear, search, user, calendar).
   - Move tenant management into a clean "House Settings" / "⚙️ Settings" button in the header bar.
2. **Component & Tree Rendering**:
   - Update `createTreeNodes` to use modern chevron SVGs and styled badge pills while preserving test selectors (`.text-green-700`, `li[data-path]`, etc.).
   - Refine Area Grid cards and Database Inspector styling to match the modern theme.
3. **Verification**:
   - Run full test suite (`pytest tests/frontend/test_v11_e2e_db.py`, `tests/test_tenant_repository_unit.py`, etc.).
   - Verify visually via live server.
