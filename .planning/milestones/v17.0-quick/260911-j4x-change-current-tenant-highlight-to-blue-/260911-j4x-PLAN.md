# Quick Task 260911-j4x: Change current tenant highlight to blue on house cards

## Goal
Switch the current tenant row highlight and icon on house overview cards from emerald green to brand blue (`bg-blue-50/70 border-blue-200/80` and `bg-blue-100 text-blue-700`), resolving the visual and semantic conflict with the `< 5 Yrs` green tenure badge.

## Tasks
1. **Update `area-grid.js`**:
   - Change `cardBg` for current tenant from `bg-emerald-50/70 border-emerald-200/80` to `bg-blue-50/70 border-blue-200/80`.
   - Change `tenantIcon` for current tenant from `bg-emerald-100 text-emerald-700` to `bg-blue-100 text-blue-700`.
   - Synchronize with `web-net/wwwroot/js/area-grid.js`.
   - Bump cache busters in `src/api/static/index.html` and `web-net/wwwroot/index.html` to `?v=260911-19`.
2. **Update Test & Verify**:
   - Update `tests/frontend/test_tenants_overview_grid.py` line 188 to match `bg-blue-50`.
   - Run Vitest, Playwright, and .NET tests.
